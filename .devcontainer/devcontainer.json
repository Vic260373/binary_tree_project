{
  "name": "BinaryTree Project",
  "image": "mcr.microsoft.com/devcontainers/cpp:1",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y cmake libgtest-dev && cd /usr/src/gtest && sudo cmake . && sudo make && sudo cp lib/*.a /usr/lib"
}
